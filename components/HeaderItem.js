const HeaderItem = ({ Icon }) => {
  return (
    <div className="group">
      <Icon className="h-5 text-purple-500 hover:opacity-50 duration-300  group-hover:animate-bounce" />
    </div>
  );
};

export default HeaderItem;
